{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend for the application",
  "main": "index.js",
  "scripts": {
    "test": "echo Backend Test Succeeded && exit 0",
    "build": "tsx build.ts",
    "serve": "node dist/bundle.js",
    "dev": "set DEV=TRUE && tsx build.ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "process": "^0.11.10"
  },
  "devDependencies": {
    "@types/node": "^24.8.1"
  }
}
